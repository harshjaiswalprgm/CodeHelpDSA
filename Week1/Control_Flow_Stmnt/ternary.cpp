#include <iostream>
using namespace std;
int main()
{

    // int age = 18;
    // (age >= 18) ? cout << "can vote " : cout << "can't vote " << endl;
    int x = 10;
    int y = 20;

    int result = (x > y) ? x : y;
    cout << "result is : " << result << endl;
    return 0;
}